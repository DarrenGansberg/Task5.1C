package com.darrengansberg.tourismapp;

//Interface to a listener that can handle clicks of a destination.
interface DestinationItemClickedListener {
    void onDestinationClicked(int destinationId);
}